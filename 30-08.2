#include <stdio.h>
#include<string.h>

int main()
{
    char s[100];
    int i,len=0;
    scanf("%s",s);
    len=sizeof(s)/sizeof(s[0]);
    for(i=0;i!=len;i++)
    {
        if(s[i]=='a')
        {
        s[i]=toupper(s[i]);
        }
    }
    printf("\n");
    printf("%s",s);

    return 0;
}
